# styles/custom/BlogTruncate.yml

extends: existence
message: "Blog posts in the `/blog` directory must contain a `<!--truncate-->` comment."
level: warning

path: blog/.*\.md$
scope: raw
tokens:
  - '<!--truncate-->'
